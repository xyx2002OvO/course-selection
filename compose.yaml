name: selection-demo

# Experiment budget. If you change cpus, also change -XX:ActiveProcessorCount to the same integer.
# Heap is 512m inside a 1g cgroup so metaspace, threads, direct buffers and AlwaysPreTouch still fit.
x-java-cpus: &java-cpus 2.0
x-java-mem: &java-mem 1g
x-java-opts: &java-opts >-
  -XX:ActiveProcessorCount=2
  -Xms512m
  -Xmx512m
  -XX:+UseContainerSupport
  -XX:+AlwaysPreTouch
  -XX:MaxMetaspaceSize=128m
  -XX:ReservedCodeCacheSize=64m
  -Djava.security.egd=file:/dev/urandom

x-app: &app
  image: selection-demo:local
  build: .
  cpus: *java-cpus
  mem_limit: *java-mem
  mem_reservation: *java-mem
  environment: &environment
    JAVA_TOOL_OPTIONS: *java-opts
    DB_URL: jdbc:mysql://mysql:3306/selection?connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true
    DB_USER: selection
    DB_PASSWORD: selection-dev
    REDIS_HOST: redis
    REDIS_PORT: 6379
    KAFKA_BOOTSTRAP: kafka:9092
    DEMO_PASSWORD: ${DEMO_PASSWORD:-demo-pass}
    NACOS_ADDR: nacos:8848
    DUBBO_PORT: 20880
    DUBBO_REGISTER_HOST: admission
  restart: unless-stopped

services:
  mysql:
    image: mysql:8.4
    cpus: 2.0
    mem_limit: 1g
    mem_reservation: 1g
    command: ["--innodb-buffer-pool-size=256M", "--max-connections=80", "--innodb-flush-log-at-trx-commit=1"]
    environment:
      MYSQL_DATABASE: selection
      MYSQL_USER: selection
      MYSQL_PASSWORD: selection-dev
      MYSQL_ROOT_PASSWORD: root-dev
    ports: ["127.0.0.1:13306:3306"]
    volumes: ["mysql-data:/var/lib/mysql"]
    healthcheck:
      test: ["CMD-SHELL", "mysql -h 127.0.0.1 -uselection -p$$MYSQL_PASSWORD selection -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 3s
      retries: 30
  redis:
    image: redis:7.4-alpine
    cpus: 1.0
    mem_limit: 256m
    mem_reservation: 256m
    command: ["redis-server", "--appendonly", "yes", "--appendfsync", "everysec", "--maxmemory", "128mb", "--maxmemory-policy", "noeviction"]
    ports: ["127.0.0.1:16379:6379"]
    volumes: ["redis-data:/data"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
  kafka:
    image: apache/kafka:4.0.2
    cpus: 1.0
    mem_limit: 768m
    mem_reservation: 768m
    ports: ["127.0.0.1:19092:19092"]
    environment:
      KAFKA_HEAP_OPTS: -Xms256m -Xmx256m
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,EXTERNAL://:19092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,EXTERNAL://localhost:19092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
      interval: 10s
      timeout: 8s
      retries: 30
  nacos:
    image: nacos/nacos-server:v2.3.2
    cpus: 1.0
    mem_limit: 768m
    mem_reservation: 768m
    environment:
      MODE: standalone
      PREFER_HOST_MODE: hostname
      JVM_XMS: 256m
      JVM_XMX: 256m
    ports: ["127.0.0.1:18848:8848"]
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8848/nacos/v1/console/health/readiness || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
  nacos-init:
    image: curlimages/curl:8.11.1
    restart: "no"
    environment:
      NACOS_ADDR: nacos:8848
    volumes:
      - ./nacos/publish.sh:/publish.sh:ro
      - ./nacos/selection-domain.yaml:/cfg/selection-domain.yaml:ro
      - ./nacos/selection-web.yaml:/cfg/selection-web.yaml:ro
    command: ["sh", "/publish.sh"]
    depends_on:
      nacos: {condition: service_healthy}
  init:
    <<: *app
    restart: "no"
    environment:
      <<: *environment
      BOOTSTRAP: "true"
      APP_JAR: /app/domain.jar
      DUBBO_ENABLED: "false"
      DUBBO_REGISTER_HOST: "127.0.0.1"
      NACOS_CONFIG_ENABLED: "false"
      NACOS_DISCOVERY_ENABLED: "false"
      WORKER_ENABLED: "false"
      SPRING_MAIN_WEB_APPLICATION_TYPE: none
    depends_on:
      mysql: {condition: service_healthy}
      redis: {condition: service_healthy}
  web:
    <<: *app
    environment:
      <<: *environment
      APP_JAR: /app/web.jar
      PORT: 18082
      NACOS_DISCOVERY_IP: web
    ports: ["127.0.0.1:18082:18082"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/18082'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 40s
    depends_on:
      init: {condition: service_completed_successfully}
      nacos-init: {condition: service_completed_successfully}
      nacos: {condition: service_healthy}
      admission: {condition: service_healthy}
      worker: {condition: service_healthy}
  api:
    <<: *app
    environment:
      <<: *environment
      APP_JAR: /app/gateway.jar
      PORT: 18080
    ports: ["127.0.0.1:18080:18080"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/18080'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 40s
    depends_on:
      nacos: {condition: service_healthy}
      nacos-init: {condition: service_completed_successfully}
      web: {condition: service_healthy}
  admission:
    <<: *app
    environment:
      <<: *environment
      APP_JAR: /app/domain.jar
      WORKER_ENABLED: "false"
      DUBBO_ENABLED: "true"
      DUBBO_REGISTER_HOST: admission
      PORT: 18083
      NACOS_DISCOVERY_IP: admission
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
    ports: ["127.0.0.1:18083:18083"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/18083'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 40s
    depends_on:
      init: {condition: service_completed_successfully}
      nacos: {condition: service_healthy}
      nacos-init: {condition: service_completed_successfully}
  worker:
    <<: *app
    environment:
      <<: *environment
      APP_JAR: /app/domain.jar
      WORKER_ENABLED: "true"
      DUBBO_ENABLED: "false"
      PORT: 18081
      NACOS_DISCOVERY_IP: worker
    ports: ["127.0.0.1:18081:18081"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/18081'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 40s
    depends_on:
      init: {condition: service_completed_successfully}
      kafka: {condition: service_healthy}
      nacos: {condition: service_healthy}
      nacos-init: {condition: service_completed_successfully}

volumes:
  mysql-data:
  redis-data:
