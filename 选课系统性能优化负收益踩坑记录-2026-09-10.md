# 选课系统性能优化负收益踩坑记录

记录日期：2026-09-10  
范围：确认事务查询合并、消费并发调整，以及后续回滚。  
结论：优化方向有合理假设，但验证不足。减少 SQL 往返和增加并发没有自动转化为端到端收益；在有限资源下，新增执行压力和排队可能抵消收益。最终恢复到此前表现较好的 12 分区、6 并发、批次 100、原规则查询版本。

## 1. 当时到底想解决什么

系统链路如下：

```text
请求进入 API
  -> Redis 原子预占
  -> accept() 同步写请求和 COMMAND Outbox
  -> 返回 HTTP 202
  -> Outbox 发布 Kafka 消息
  -> confirm() 校验规则、扣库存、写选课记录和 RESULT Outbox
  -> Redis 投影终态
```

最初的明显瓶颈是 Outbox 逐条等待 Kafka 发送完成。改为批量发送后，报告中的确认窗口均值从约 20/s 提升到约 79/s，COMMAND Outbox 能正常发出。这次优化确实解决了一个已观察到的阻塞点。

接下来，受理速度高于确认速度，待确认请求不断积压。于是尝试继续优化 `confirm()`，目标是提高最终落库速度，同时不明显损害入口延迟。

本机资源很有限：API、Worker、MySQL 各 2 核；API 和 Worker 各 1Gi 内存，MySQL 1Gi、缓冲池 256Mi；Kafka 只有 1 核。API 和 Worker 是两个进程，但共用同一个 MySQL。整套服务还运行在同一台 Docker Desktop 主机上。

## 2. 为什么当时觉得改动会有用

### 假设一：三次查询合成一次，可以缩短事务

原确认事务会分别查询目标课程、该学生本学期全部已选课程、该学生全部已修课程，然后在 Java 中判断先修课、时间冲突、互斥和学分限制。

改动后，用一条包含 JOIN、SUM、MIN、MAX 和 EXISTS 的查询返回规则摘要。成功确认路径的业务 SQL 从 9 次减少为 7 次。

当时的预期是：少两次数据库往返，减少传输的行和 Java 对象，进而缩短事务与持锁时间。这是值得实验的假设，尤其是在网络往返比较贵的时候。

但它依赖一个前提：新增的数据库计算成本，小于省下的往返和应用计算成本。当时没有先测清这个前提。

### 假设二：消费者没有跑满 CPU，提高并发能填补等待时间

早期消费并发为 3，Worker CPU 没有持续打满。数据库访问包含等待，因此增加同时处理的请求，理论上可能提高吞吐。Kafka 分区从 3 增加到 12，也为更多消费者提供了分配空间。

当时忽略了一个区别：Worker 自己还有 CPU 余量，不代表共享 MySQL 还有处理余量。增加分区也不会增加数据库算力。

### 假设三：更大的批次能摊薄固定开销

增大拉取数量和 Outbox 批次，可以摊薄轮询、领取和发送的固定开销。此前 Outbox 批量化成功，更容易让人继续沿着“批次更大、并发更多”的方向调整。

但 Outbox 的收益来自消除了明确的串行等待，不能据此推断确认事务也应该不断加并发。后者包含数据库写入、锁和事务提交，是另一种负载。

## 3. 实际发生了什么

以下数据来自原压测报告和当时保留版本的执行记录。不同轮次存在课程分布、配置和采样差异，不能当作严格单变量实验。

| 轮次 | 主要配置或变化 | 受理表现 | 确认表现 |
| --- | --- | --- | --- |
| Outbox 批量后的早期基线 | 3 分区、3 并发、原规则查询、单课 | 约 99.7%，P95 约 248ms | 停压快照 SUCCESS 5903，按 75 秒估算约 79/s |
| 后来选择回滚到的版本 | 12 分区、6 并发、批次 100、原规则查询、多课 | 98.41%，P95 680.56ms，丢弃迭代 186 | 停压 SUCCESS 14961；随后 20 秒记录为全部 24565 成功 |
| 更激进的并发试验 | 并发 12、max-poll 100、批次 200 | 1 秒内成功率降到 24.49% | 后台消化更积极，但入口明显受损，因此未保留 |
| 报告第 5 轮 | 合并查询、12 分区、6 并发、批次 100 | 24185 次请求，1 秒内 202 为 0；API 有连接池超时 | 停压 SUCCESS 4076，窗口估算约 54/s；停压后阶段约 150–200/s |

第 5 轮中，API CPU 峰值约 212%，Worker 194%，MySQL 193%，Kafka 108%。这里 200% 大致对应使用两颗 CPU 核，说明多个环节都接近配额，并不表示机器有“额外的 200% 容量”。

必须特别说明：6 并发曾经取得较好结果，后来同样 6 并发又失败。因此，“6 并发本身一定错误”不是证据支持的结论。同理，后一次失败也不能仅凭时间先后，就证明全部退化都来自合并 SQL。

后续还有一轮采集试跑与集成测试重叠，不能作为公平对照。独立运行的 A 组使用原查询、并发 3，稳态按时受理约 202 次/秒、成功率约 51.2%，确认采样约 79/s，也没有复现最早的入口表现。这进一步说明历史轮次之间的运行状态和实验条件不能视为完全相同。

## 4. 为什么优化可能反而拖慢了系统

### 4.1 少发两条 SQL，不等于少做数据库工作

原方案将一部分判断放在 Java 中。合并后，这些工作转移到 MySQL：联表、计算聚合值、判断冲突、查询先修关系。返回的数据少了，但数据库内部的工作未必少。

对于本来只选了零门或少量课程的学生，原查询返回的数据已经很小。合并 SQL 省下的传输和对象分配可能有限，而数据库仍要执行新的查询计划。对于已选课程更多的学生，其成本又会变化。

因此，应比较的是：

```text
节省的网络往返和应用处理成本
与
新增的数据库计算、执行计划及排队成本
```

本次没有完整的同并发查询 A/B、执行计划和 SQL 耗时证据，不能写成“聚合必然更慢”或“确定出现了全表扫描”。准确的结论是：优化没有证明自己更便宜，也没有证明自己缩短了事务。

### 4.2 API 与 Worker 争用的是同一个数据库

API 返回 202 之前仍要同步写 MySQL。所以后台确认负载上升，会影响前台受理所依赖的数据库。

可以用下面的过程解释观察到的现象：

```text
后台数据库并发或每笔执行成本上升
  -> 共享数据库响应变慢
  -> API 事务持有连接的时间变长
  -> API 自己的连接池用满，更多请求等待连接
  -> 请求总耗时超过 1 秒，客户端超时
```

API 日志中出现了 `total=16, active=16` 的 Hikari 连接获取超时。这能证明 API 当时发生了连接池饱和，但仅凭这一条日志，无法区分 CPU、存储提交、锁等待等因素各占多少。

这里也要纠正一种不准确的说法：不是“Worker 抢走了 API 的 16 条连接”。两个进程各自拥有连接池，Worker 争用的是共享 MySQL 的处理资源，间接使 API 的连接不能及时归还。

### 4.3 查询合并没有改变事务边界和关键锁

优化后仍然是一个请求一个事务，仍需锁请求、锁学生、条件扣库存、写 enrollment、更新状态、写 RESULT Outbox，最后提交。

锁没有因为 SQL 数量减少而提前释放。如果合并查询本身更慢，或者数据库排队更长，事务内已有的锁反而会持有更久。

课程库存行仍然可能是热点，但本次没有证据证明它主导了所有退化。早期多课与单课确认速度相近，也不支持一开始就把主要问题认定为单课程行锁。

### 4.4 停压排空更快，掩盖了带压时的退化

停压之后，API 不再持续争用数据库，Worker 能得到更多资源。此时排空速度上升，与有入口流量时的确认速度并不是同一个指标。

“最终都落库了”也不能抹掉客户端超时。客户端已经等到失败，而服务器随后提交成功，这属于结果不确定的请求体验，需要幂等重试和状态查询来处理。

因此，历史记录中的约 199/s 是停压快照除以 75 秒的窗口估算，约 480/s 是所记载的 20 秒排空区间估算。它们都不能直接当作稳定确认容量，更不能与 HTTP 受理 QPS 合成一个“系统 QPS”。

## 5. 真正踩的坑是验证方法

1. **把代理指标当成目标。** SQL 条数、消费者数量、排空速度都是中间指标；用户关心的是按时受理、最终成功和等待时间。
2. **同时调整了多个变量。** 查询、并发、批次、课程分布和超时条件交织变化，出现退化后难以定位原因。
3. **没有先验证成本转移。** 把工作从 Java 搬到只有 2 核的 MySQL，未必是优化；数据库恰好还是前后台共享资源。
4. **过早使用了肯定措辞。** 当时应该说“减少了两次 SQL 往返，性能待验证”，而不应在缺少耗时证据时，将它描述成已经实现事务瘦身。
5. **对压测条件控制不足。** 同机集成测试、冷启动、JIT、采样开销和发生器丢弃迭代，都可能影响结果。一次运行不够证明稳定收益。
6. **混淆了正确性和性能。** 测试通过说明覆盖的业务行为没有出错，不说明吞吐提高；REJECTED 为零也不代表受理质量好，更不能独立证明无超卖。

## 6. 最后如何处理

按此前较好版本恢复了以下行为和配置：

- Kafka requests、results 及 DLT 保留 12 分区。
- Listener 并发恢复为 6，`max-poll-records=50`，Outbox 批次 100。
- loadtest 的 API 和 Worker 连接池均恢复为 16。
- 确认事务恢复原规则查询；受理恢复插入后锁定读取的路径。
- 撤回后续合并查询、快速受理和积压保护，保留历史实验资料。
- 恢复 `15s@50 + 60s@400`、多课、学生段错开、1 秒超时的压测场景。

回滚后 `mvn -U verify` 通过：16 个单元测试、11 个集成测试。这个结果证明回滚后的代码通过了相应校验，不表示已经重新复现 98.41% 的受理成功率。本次回滚验证没有重新压测，也没有重建正在运行的业务容器。

快速受理与积压保护属于后续探索，不能因为一并撤回，就认定它们也是已证实的负优化；其中积压保护本来就可能主动拒绝流量，需要与无保护的容量测试分开评价。

## 7. 下次怎么避免重演

先冻结代码版本、资源配额、测试数据、超时、预热方式和采样方式，在同一条件下稳定复现基线。独立运行压测，不与构建或集成测试争用主机。

然后一次只改一个变量：同并发比较原查询与合并查询，再比较消费并发，最后单独比较连接池和批次。查询实验覆盖已选 0、5、10 门课的学生，记录执行计划、扫描行数、查询耗时和完整事务耗时。

每组至少重复三次，同时看按时 202 数、P95/P99、超时、丢弃迭代、稳态 SUCCESS 增量、积压年龄、取消数量及最终一致性。采用固定采样间隔并使用实际时间差计算确认速率；排空数据单列。

先约定验收标准，例如稳态按时受理率至少 99%、零丢弃迭代，并要求终态延迟和正确性不退化。若采用这一标准，历史上 98.41% 的版本也只能称为相对更好的回滚点，不能称为已经达标。

这次最值得记住的一点是：**优化一个环节时，要追踪它把工作和等待转移到了哪里。省下的 SQL 次数和增加的并发量，只有在完整链路中改善了用户可见结果，才算性能收益。**

## 依据

- 项目压测报告：`loadtest/out/REPORT.md`。
- 对话中留存的 6 并发保守版本和 12 并发试验结果。
- A 组记录：`loadtest/out/20260909-223107-bc97b757/`；采集试跑 `20260909-222501-4712d496/` 与集成测试重叠，不用于公平对比。
- 回滚前备份：`loadtest/out/before-rollback-20260910-101531.zip`。
- 2026-09-10 回滚后的 Maven 验证输出。本文没有将缺少单变量验证的推断写成已定位的根因。
